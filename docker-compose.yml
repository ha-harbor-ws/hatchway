# Запуск на Linux: создайте .env из .env.example, затем:
#   docker compose up -d --build
services:
  web:
    build: .
    image: hatchway:latest
    container_name: hatchway
    hostname: hatchway
    restart: unless-stopped
    ports:
      - "${HATCHWAY_PORT:-8000}:8000"
    environment:
      TZ: Europe/Moscow
      SESSION_SECRET: ${SESSION_SECRET:?Set SESSION_SECRET in .env file}
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-25}
    volumes:
      - hatchway_data:/app/data
      - hatchway_uploads:/app/uploads
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

volumes:
  hatchway_data:
  hatchway_uploads:
