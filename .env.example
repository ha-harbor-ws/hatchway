# Скопируйте в .env и задайте секрет (длинная случайная строка)
SESSION_SECRET=замените-на-случайную-строку-минимум-32-символа

# Порт на хосте (контейнер внутри всегда 8000)
HATCHWAY_PORT=8000

# Необязательно
MAX_UPLOAD_MB=25
